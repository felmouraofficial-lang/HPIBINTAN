export const dynamic = "force-dynamic";

import Image from "next/image"; import { SiteHeader } from "@/components/site-header"; import { SiteFooter } from "@/components/site-footer"; import { PageHero } from "@/components/page-hero"; import { Card } from "@/components/ui/card"; import { prisma } from "@/lib/prisma"; import { fallbackMembers } from "@/lib/fallback-data";
export default async function MembersPage() { let members: any[] = fallbackMembers; try { const dbMembers = await prisma.member.findMany({ where: { isActive: true }, orderBy: { name: "asc" } }); members = dbMembers.length ? dbMembers : fallbackMembers; } catch {} return <><SiteHeader /><PageHero title="Data Anggota" description="Direktori anggota dan pengurus HPI Pulau Bintan Kepulauan Riau." /><main className="container grid gap-5 py-12 sm:grid-cols-2 lg:grid-cols-3">{members.map((m) => <Card key={m.id}><Image src={m.photo || "/logos/hpi-logo-1.jpeg"} alt={m.name} width={360} height={260} className="h-56 w-full rounded-lg object-cover" /><h2 className="mt-4 text-xl font-black">{m.name}</h2><p className="text-primary">{m.position}</p>{m.contact && <p className="mt-2 text-sm text-zinc-600">{m.contact}</p>}</Card>)}</main><SiteFooter /></>; }
