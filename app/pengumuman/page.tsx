export const dynamic = "force-dynamic";

import { SiteHeader } from "@/components/site-header"; import { SiteFooter } from "@/components/site-footer"; import { PageHero } from "@/components/page-hero"; import { Card } from "@/components/ui/card"; import { prisma } from "@/lib/prisma"; import { formatDate } from "@/lib/utils"; import { fallbackAnnouncements } from "@/lib/fallback-data";
export default async function AnnouncementsPage() { let data: any[] = fallbackAnnouncements; try { const dbData = await prisma.announcement.findMany({ where: { isPublished: true }, orderBy: { publishedAt: "desc" } }); data = dbData.length ? dbData : fallbackAnnouncements; } catch {} return <><SiteHeader /><PageHero title="Pengumuman" description="Informasi terbaru untuk anggota dan masyarakat." /><main className="container grid gap-4 py-12">{data.map((a) => <Card key={a.id}><p className="text-sm text-zinc-500">{formatDate(a.publishedAt)}</p><h2 className="mt-1 text-xl font-black">{a.title}</h2><p className="mt-2 leading-7 text-zinc-600">{a.content}</p></Card>)}</main><SiteFooter /></>; }
