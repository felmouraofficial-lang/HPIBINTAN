export const dynamic = "force-dynamic";

import Image from "next/image"; import { SiteHeader } from "@/components/site-header"; import { SiteFooter } from "@/components/site-footer"; import { PageHero } from "@/components/page-hero"; import { Card } from "@/components/ui/card"; import { prisma } from "@/lib/prisma";
export default async function TransportPage() { let data: any[] = []; try { data = await prisma.transportation.findMany({ orderBy: { name: "asc" } }); } catch {} return <><SiteHeader /><PageHero title="Layanan Transportasi" description="Informasi kendaraan, kapasitas, status, dan deskripsi layanan." /><main className="container grid gap-5 py-12 sm:grid-cols-2 lg:grid-cols-3">{data.length ? data.map((t) => <Card key={t.id}><Image src={t.photo || "/logos/hpi-logo-2.jpeg"} alt={t.name} width={420} height={280} className="h-52 w-full rounded-lg object-cover" /><h2 className="mt-4 text-xl font-black">{t.name}</h2><p className="text-sm text-zinc-500">{t.vehicleType} - {t.capacity} orang</p><span className="mt-3 inline-block rounded-lg bg-yellow-100 px-3 py-1 text-xs font-bold text-yellow-800">{t.status === "AVAILABLE" ? "Tersedia" : "Tidak Tersedia"}</span><p className="mt-3 text-sm leading-6 text-zinc-600">{t.description}</p></Card>) : <Card className="sm:col-span-2 lg:col-span-3">Belum ada data transportasi. Tambahkan melalui Dashboard Admin.</Card>}</main><SiteFooter /></>; }
