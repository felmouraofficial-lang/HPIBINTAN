export const dynamic = "force-dynamic";

import Image from "next/image";
import Link from "next/link";
import { ArrowRight, CalendarDays, Camera, Compass, Gem, Lightbulb, Megaphone, Navigation, Users } from "lucide-react";
import { SiteHeader } from "@/components/site-header";
import { SiteFooter } from "@/components/site-footer";
import { Card } from "@/components/ui/card";
import { OrganizationMotion } from "@/components/organization-motion";
import { DestinationSection } from "@/components/destination-section";
import { prisma } from "@/lib/prisma";
import { formatDate, logos } from "@/lib/utils";

type Destination = { category: string; name: string; description: string; location: string; image: string; mapUrl?: string };

async function getHomeData() {
  try {
    const [profile, members, announcements, meetings, gallery, docs, transport, contact, settings] = await Promise.all([
      prisma.organizationProfile.findFirst(),
      prisma.member.findMany({ where: { isActive: true }, orderBy: { createdAt: "asc" }, take: 8 }),
      prisma.announcement.findMany({ where: { isPublished: true }, orderBy: { publishedAt: "desc" }, take: 3 }),
      prisma.meeting.findMany({ orderBy: { date: "asc" }, take: 3 }),
      prisma.gallery.findMany({ orderBy: { createdAt: "desc" }, take: 6 }),
      prisma.documentation.findMany({ orderBy: { createdAt: "desc" }, take: 4 }),
      prisma.transportation.findMany({ orderBy: { createdAt: "desc" }, take: 4 }),
      prisma.contact.findFirst(),
      prisma.settings.findMany(),
    ]);
    const setting = Object.fromEntries(settings.map((s) => [s.key, s.value]));
    return { profile, members, announcements, meetings, gallery, docs, transport, contact, setting, destinations: parseJson<Destination[]>(setting.destinations, []), partners: parseJson<string[]>(setting.partners, []) };
  } catch {
    return { profile: null, members: [], announcements: [], meetings: [], gallery: [], docs: [], transport: [], contact: null, setting: {}, destinations: [], partners: [] };
  }
}
function parseJson<T>(value: string | undefined, fallback: T): T { try { return value ? JSON.parse(value) as T : fallback; } catch { return fallback; } }
export default async function HomePage() {
  const { profile, members, announcements, meetings, gallery, transport, contact, setting, destinations, partners } = await getHomeData();

  return <><SiteHeader /><main className="overflow-hidden bg-[#fffaf3]"><section className="relative min-h-[590px] bg-zinc-950 text-white"><Image src={setting.hero_image || "/hero-bintan.jpg"} alt="Wisata Pulau Bintan" fill priority className="object-cover cinematic-hero-still" /><video className="absolute inset-0 h-full w-full object-cover" autoPlay muted loop playsInline aria-label="Video cinematic Pulau Bintan"><source src="/hero-bintan.mp4" type="video/mp4" /></video><div className="absolute inset-0 bg-black/35" /><div className="absolute inset-0 bg-gradient-to-r from-black/78 via-black/45 to-black/10" /><div className="container relative flex min-h-[590px] items-center pt-28 pb-12"><div className="max-w-3xl"><p className="inline-flex items-center gap-3 rounded-full border border-white/25 bg-white/15 px-4 py-2 text-sm font-bold backdrop-blur"><span className="h-3 w-3 rounded-full bg-[#ff9900]" />Organisasi Profesi Pramuwisata</p><h1 className="mt-6 text-balance text-4xl font-black leading-[1.06] md:text-6xl">HPI<br /><span className="text-[#ff9900]">PULAU BINTAN</span></h1><p className="mt-4 max-w-2xl text-base font-semibold leading-8 text-zinc-100 drop-shadow md:text-lg">Himpunan Pramuwisata Indonesia Pulau Bintan Kepulauan Riau - wadah pramuwisata profesional, lokal, terpercaya, dan berpengalaman.</p><div className="mt-7 flex flex-wrap gap-3"><Link href="/anggota" className="inline-flex items-center gap-4 rounded-full bg-[#ff9900] px-6 py-3 text-base font-black text-white shadow-soft transition duration-300 hover:-translate-y-1 hover:bg-primary hover:shadow-[0_22px_50px_rgba(255,153,0,.28)]">Daftar Anggota <ArrowRight className="h-5 w-5" /></Link><Link href="/tentang-kami" className="inline-flex items-center gap-4 rounded-full border-2 border-white/35 bg-[#b9ad98]/28 px-6 py-3 text-base font-black text-white backdrop-blur transition duration-300 hover:-translate-y-1 hover:border-white/70 hover:bg-[#ff9900]/90 hover:shadow-[0_22px_50px_rgba(255,255,255,.16)]">Tentang Kami</Link></div><div className="mt-8 h-px max-w-2xl bg-white/25" /><div className="mt-6 grid max-w-2xl grid-cols-3 gap-5">{[[Users, "Anggota Aktif", "100+"], [CalendarDays, "Kegiatan/Tahun", "60+"], [Navigation, "Mitra Kerja", "46+"]].map(([Icon, label, value]) => <div key={label as string}><Icon className="h-7 w-7 text-[#ff9900]" /><div className="mt-2 text-3xl font-black md:text-4xl">{value as string}</div><p className="mt-1 text-xs font-black uppercase tracking-[.16em] text-zinc-300">{label as string}</p></div>)}</div></div></div></section><section className="bg-white py-20"><div className="container grid gap-10 lg:grid-cols-[.9fr_1.1fr]"><div><SectionTitle eyebrow="Tentang Organisasi" title="Mengawal profesionalisme pramuwisata Bintan." /><p className="mt-6 text-lg leading-9 text-zinc-600">{profile?.history}</p><Link href="/tentang-kami" className="mt-8 inline-flex rounded-full bg-[#8a4719] px-7 py-4 font-black text-white">Selengkapnya</Link></div><div className="grid gap-5">{([{ title: "Visi", text: profile?.vision, Icon: Compass, gradient: "from-sky-500 to-cyan-400" }, { title: "Misi", text: profile?.mission, Icon: Lightbulb, gradient: "from-amber-500 to-orange-400" }, { title: "Nilai", text: "Profesionalisme, integritas, pelayanan prima, kolaborasi, dan pengembangan berkelanjutan.", Icon: Gem, gradient: "from-emerald-500 to-teal-400" }]).map(({ title, text, Icon, gradient }) => <Card key={title} className="group flex gap-5 p-7 transition duration-300 hover:-translate-y-1 hover:shadow-[0_24px_60px_rgba(41,20,8,.12)]"><div className={`grid h-16 w-16 shrink-0 place-items-center rounded-2xl bg-gradient-to-br ${gradient} text-white shadow-soft transition duration-300 group-hover:scale-110`}><Icon className="h-8 w-8" /></div><div><h3 className="text-2xl font-black">{title}</h3><p className="mt-2 leading-7 text-zinc-600">{text}</p></div></Card>)}</div></div></section><section className="bg-white py-16"><div className="container"><SectionTitle center eyebrow="Struktur Organisasi" title="Pengurus Inti HPI Pulau Bintan" subtitle="Sinergi profesional untuk pariwisata unggul" /><div className="mt-10 rounded-[2rem] bg-[#fff8ed] p-5"><OrganizationMotion members={members} /></div></div></section><DestinationSection destinations={destinations} mapImage={setting.map_image ?? "/peta-pulau-bintan.jpg"} mapText={setting.map_text} /><section className="bg-white py-20"><div className="container grid gap-10 lg:grid-cols-[.75fr_1.25fr]"><div><SectionTitle eyebrow="Agenda & Info" title="Kegiatan dan pengumuman terbaru." />{announcements.map((a) => <div key={a.id} className="mt-6 border-b border-zinc-200 pb-5"><Megaphone className="h-5 w-5 text-primary" /><h3 className="mt-3 text-xl font-black">{a.title}</h3><p className="mt-2 text-sm leading-6 text-zinc-600">{a.content}</p></div>)}</div><div className="grid gap-5 md:grid-cols-3">{meetings.map((m) => <Card key={m.id}><CalendarDays className="text-primary" /><p className="mt-5 text-sm font-bold text-zinc-500">{formatDate(m.date)} - {m.time}</p><h3 className="mt-3 text-xl font-black">{m.title}</h3><p className="mt-2 text-sm font-bold text-primary">{m.location}</p></Card>)}</div></div></section><section className="py-20"><div className="container"><SectionTitle center eyebrow="Galeri" title="Dokumentasi Kegiatan" /><div className="mt-10 grid gap-5 md:grid-cols-3">{gallery.map((g) => <div key={g.id} className="group overflow-hidden rounded-[2rem] bg-white shadow-soft"><Image src={g.fileUrl} alt={g.title} width={700} height={460} className="h-64 w-full object-cover transition duration-500 group-hover:scale-110" /><div className="p-5"><Camera className="h-5 w-5 text-primary" /><h3 className="mt-3 font-black">{g.title}</h3></div></div>)}</div></div></section><section className="bg-white py-20"><div className="container"><SectionTitle center eyebrow="Transportasi" title="Layanan Transportasi Mitra" /><div className="mt-10 grid gap-5 md:grid-cols-4">{transport.map((t) => <Card key={t.id} className="overflow-hidden p-0"><Image src={t.photo || logos[1]} alt={t.name} width={700} height={430} className="h-44 w-full object-cover" /><div className="p-5"><span className="rounded-full bg-yellow-100 px-3 py-1 text-xs font-black text-yellow-800">{t.status === "AVAILABLE" ? "Tersedia" : "Tidak tersedia"}</span><h3 className="mt-4 font-black">{t.name}</h3><p className="mt-2 text-sm text-zinc-500">{t.vehicleType} - {t.capacity} penumpang</p></div></Card>)}</div></div></section><section className="bg-zinc-950 py-20 text-white"><div className="container grid gap-10 lg:grid-cols-[.7fr_1.3fr]"><SectionTitle inverse eyebrow="Partner" title="Kolaborasi untuk ekosistem wisata Bintan." /><div className="grid grid-cols-2 gap-4 md:grid-cols-3">{[...partners, ...logos].map((partner, index) => <div key={`${partner}-${index}`} className="grid h-24 place-items-center rounded-3xl border border-white/10 bg-white/10 p-4 text-center font-black text-zinc-200 backdrop-blur">{String(partner).startsWith("/") ? <Image src={String(partner)} alt="Logo partner" width={70} height={70} className="object-contain" /> : partner}</div>)}</div></div></section><section className="bg-white py-16"><div className="container grid gap-5 md:grid-cols-2"><div><SectionTitle eyebrow="Absensi Staff" title="Tools absensi harian dan rapat." subtitle="Akses cepat untuk staff melakukan absensi tanpa masuk dashboard admin." /></div><div className="grid gap-4 sm:grid-cols-2"><Link href="/absensi-harian" className="rounded-3xl border border-zinc-200 bg-[#fff8ed] p-6 shadow-soft transition hover:-translate-y-1"><CalendarDays className="h-8 w-8 text-primary" /><h3 className="mt-5 text-xl font-black text-[#2f2119]">Absensi Harian</h3><p className="mt-2 text-sm leading-6 text-zinc-600">Check in/check out staff dengan upload foto bukti kehadiran.</p><span className="mt-5 inline-flex items-center gap-2 text-sm font-black text-primary">Buka Form <ArrowRight className="h-4 w-4" /></span></Link><Link href="/absensi-rapat" className="rounded-3xl border border-zinc-200 bg-[#fff8ed] p-6 shadow-soft transition hover:-translate-y-1"><Users className="h-8 w-8 text-primary" /><h3 className="mt-5 text-xl font-black text-[#2f2119]">Absensi Rapat</h3><p className="mt-2 text-sm leading-6 text-zinc-600">Staff mengisi kehadiran sesuai jadwal rapat yang dibuat admin.</p><span className="mt-5 inline-flex items-center gap-2 text-sm font-black text-primary">Buka Form <ArrowRight className="h-4 w-4" /></span></Link></div></div></section><section className="bg-primary py-16 text-white"><div className="container grid gap-8 lg:grid-cols-[1fr_auto]"><div><SectionTitle inverse eyebrow="Kontak" title="Guide booking dan informasi resmi." /><p className="mt-5 flex items-center gap-2 text-lg font-bold"><Navigation className="h-5 w-5" />{contact?.address}</p><p className="mt-2 text-red-50">{contact?.email} - {contact?.whatsapp}</p></div><Link href="/kontak" className="inline-flex h-14 items-center justify-center rounded-full bg-white px-8 text-sm font-black uppercase tracking-[.18em] text-primary">Hubungi Sekretariat</Link></div></section></main><SiteFooter /></>;
}

function SectionTitle({ eyebrow, title, subtitle, inverse = false, center = false }: { eyebrow: string; title: string; subtitle?: string; inverse?: boolean; center?: boolean }) {
  return <div className={center ? "mx-auto max-w-3xl text-center" : ""}><p className={`text-sm font-black uppercase tracking-[.22em] ${inverse ? "text-gold" : "text-gold-deep"}`}>{eyebrow}</p><h2 className={`mt-3 text-balance text-4xl font-black leading-tight md:text-5xl ${inverse ? "text-white" : "text-[#2f2119]"}`}>{title}</h2>{subtitle && <p className={`mt-4 text-lg ${inverse ? "text-zinc-200" : "text-zinc-500"}`}>{subtitle}</p>}</div>;
}
