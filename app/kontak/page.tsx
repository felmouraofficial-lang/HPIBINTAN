export const dynamic = "force-dynamic";

import { Mail, MapPin, MessageCircle } from "lucide-react"; import { SiteHeader } from "@/components/site-header"; import { SiteFooter } from "@/components/site-footer"; import { PageHero } from "@/components/page-hero"; import { Card } from "@/components/ui/card"; import { prisma } from "@/lib/prisma";
export default async function ContactPage() { let contact: any = null; try { contact = await prisma.contact.findFirst(); } catch {} return <><SiteHeader /><PageHero title="Kontak" description="Hubungi HPI Pulau Bintan Kepulauan Riau untuk informasi organisasi dan layanan pramuwisata." /><main className="container grid gap-5 py-12 lg:grid-cols-3"><Card><MapPin className="text-primary" /><h2 className="mt-4 font-black">Alamat</h2><p className="mt-2 text-sm leading-6 text-zinc-600">{contact?.address ?? "Kabupaten Bintan, Kepulauan Riau"}</p></Card><Card><MessageCircle className="text-primary" /><h2 className="mt-4 font-black">WhatsApp</h2><p className="mt-2 text-sm text-zinc-600">{contact?.whatsapp ?? "-"}</p></Card><Card><Mail className="text-primary" /><h2 className="mt-4 font-black">Email</h2><p className="mt-2 text-sm text-zinc-600">{contact?.email ?? "-"}</p></Card>{contact?.mapUrl && <iframe src={contact.mapUrl} className="h-96 w-full rounded-xl border lg:col-span-3" loading="lazy" />}</main><SiteFooter /></>; }
